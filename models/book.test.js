const request = require('supertest');
const express = require('express');
const app = express();
app.use(express.json());

describe('Test GET / methods for get all books Available in Database ', () => {
    beforeAll(async () => {

    });
    test('test find all ', () => {

    })
})